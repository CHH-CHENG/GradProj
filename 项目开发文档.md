**项目开发推进文档说明**

本文件用于记录项目的：

1.  项目目标与研究动机

2.  项目整体设计与最终框架

3.  各模块职责与代码结构说明

核心作用：

让任何一个新的开发者（包括 AI），在不阅读代码的情况下，\
通过本文件能够快速理解（或者引导到正确位置）：

- 这个项目在做什么

- 当前已经做到了哪一步

- 下一步应该做什么

- 如何继续开发并保持规范

⚠️ 强制要求：

所有代码结构修改 / 新增模块
/逻辑调整，必须同步记录在开发日志中（DEVELOP_LOG.md）。

**1. 项目目标与研究动机**

**1.1 项目目标**

构建一个基于 Sentinel-2
遥感数据的森林蓄积量估测系统（本科毕设-基于Sentinel-2多源遥感特征与随机森林的森林蓄积量估测研究），实现：

- 自动化遥感数据下载

- 遥感影像预处理

- 多源特征提取

- 机器学习建模（随机森林）

- 模型解释（SHAP）

- 蓄积量空间预测

**1.2 问题定义**

本项目为回归问题：

输入（X）：

- 光谱特征

- 植被指数

- 纹理特征

- 地形特征

输出（y）：

- 林分蓄积量（m³/ha）

**1.3 出发点**

传统森林蓄积量获取依赖人工调查，成本高、周期长。

本项目希望通过遥感 + 机器学习，实现：

- 自动化估测

- 空间连续预测

- 提高效率

**2. 项目最终框架（总设计）**

**2.1 总流程**

样地数据 → 特征提取 → 建模 → 预测

遥感数据 → 预处理 → 特征提取 → 融合 → 建模

**2.2 数据流**

Sentinel-2 → zip → SAFE → ROI → 特征 → samples.csv → 模型

**2.3 项目原则**

1.  模块化（每一步独立）

2.  可重复运行（不重复下载/处理）

3.  数据与代码分离

4.  所有中间结果可落盘

**3. 项目结构说明**

**3.1 总体结构**

ProjCode/

├── config/ \# 配置文件（账号 / 参数；spain_study.yaml 西班牙研究区配置）

├── data/ \# 数据目录

├── downloader/ \# 数据下载器（copernicus.py / spain.py / estimate.py）

├── preprocess/ \# 数据预处理（unzip.py / read_bands.py / crop_roi.py）

├── feature/ \# 特征工程（extract.py / subsample.py）

├── model/ \# 模型训练与评估（rf.py）

├── utils/ \# 通用工具

├── 项目开发文档.md #当前文件，项目详细信息以及正确引导

├── DEVELOP_LOG.md #开发日志

├── DEVELOP_LOG_ARCHIVE.md #历史开发日志归档（初始化 / 秦岭 / 芬兰阶段）

├── README.md #项目概览

├── environment.yaml #conda环境配置文件

└── main.py \# 主流程控制入口

**3.2 各模块脚本级说明**

🔹 downloader/（数据下载）

copernicus.py

功能：

- Copernicus API 认证（token获取与刷新）

- Sentinel-2 产品搜索

- 产品筛选（时间 / 经纬度 / 云量）

- 数据下载（支持断点续传）

- 自动重试机制

输入：

- config/copernicus.yaml（账号、查询参数）

输出：

- data/Sentinel2/zip/\*.zip

要求：

- 必须支持断点续传（.part 文件机制）

- 必须校验下载完整性（zipfile.is_zipfile）

- 禁止重复下载已存在文件

- token过期必须自动刷新（401处理）

spain.py

功能：

- 按省份（leon / burgos / lugo）采集 Sentinel-2 L2A（整省矩形查询 → 按 tile 去重 → 下载）

输入：

- downloader/copernicus.py 的查询/下载能力

- 省份 bbox 与时间窗（脚本内 REGIONS 定义，与 IFN4 调查年匹配）

输出：

- data/Sentinel2/zip/Spain_IFN4/<省>/<UUID>.zip

用法：

- python -m downloader.spain all ／ --dry-run leon

estimate.py

功能：

- 下载前估算查询结果的产品总大小（按 tile 去重后统计 GB）

用法：

- python -m downloader.estimate

dem.py

功能：

- 从 CNIG/IDEE WCS-INSPIRE（免账号）按省份 bbox 分块下载西班牙 DEM（GeoTIFF）

输入：

- config/spain_study.yaml 的三省 bbox；WCS 服务 `https://servicios.idee.es/wcs-inspire/mdt`

输出：

- data/DEM/raw/Spain_DEM/<省>/mdt{05|25}/（GeoTIFF 分块 + _manifest.csv 清单）

要求：

- 幂等（已下载块跳过）、防缺漏（清单校验）、失败重试

🔹 preprocess/（预处理）

unzip.py

功能：

- 扫描 zip 文件

- 解压为 SAFE 目录

- 自动识别 SAFE 名称（避免 UUID 错误）

- 跳过已解压产品

输入：

- data/Sentinel2/zip/\*.zip

输出：

- data/Sentinel2/SAFE/\*.SAFE/

要求：

- 必须通过 zip 内部结构识别 SAFE 名

- 禁止重复解压

- 解压失败需删除残留文件

read_bands.py

功能：

- 读取 Sentinel-2 波段（JP2）

- 支持 L2A 数据结构（R10m/R20m/R60m）

- 波段重采样（统一到10m）

- 按 ROI 窗口读取（避免全图加载）

- DN → 反射率（0\~1）

输入：

- SAFE 目录

输出：

- numpy数组 / rasterio对象

要求：

- 必须支持窗口读取（节省内存）

- 必须统一分辨率（10m）

- 必须标准化数值范围

crop_roi.py

功能：

- 根据研究区裁剪影像

- 支持两种模式：

  - 固定ROI（中心点+缓冲区）

  - 样地ROI（矢量裁剪）

- 输出多波段 GeoTIFF

输入：

- SAFE 数据

- config/preprocess.yaml

输出：

- data/Sentinel2/

要求：

- 输出必须统一：

  - CRS一致

  - 分辨率10m

  - 波段顺序固定（B02\~B12）

- 必须支持批处理（多个SAFE）

🔹 feature/（特征工程）

extract.py

功能：

- 从 ROI 影像提取特征

- 生成训练数据 samples.csv

特征类型（研究方案 v1，共 **23** 个 —— 详见 8.11）：

1.  基础光谱（10）：B02 B03 B04 B05 B06 B07 B08 B8A B11 B12

2.  植被指数（3）：NDVI / NDWI / NDRE

3.  多尺度统计（3）：NDVI_std50 / NDRE_std50 / B8_std50

4.  结构-纹理（2）：GLCM_contrast / GLCM_entropy

5.  结构-邻域（2）：NDVI_mean100 / NDVI_std100

6.  地形（3）：DEM_elev / DEM_slope / DEM_aspect

输入：

- data/Sentinel2/

- data/inventory/

输出：

- data/feature/

要求：

- 每个样地对应一行

- 必须包含：

  - 特征X

  - 标签y（蓄积量）

- 必须处理缺失值（NaN）

subsample.py

功能：

- 从 samples.csv 按 province（省份）分别做空间分箱抽样，降低空间自相关

输入：

- data/feature/samples.csv

输出：

- data/feature/samples_sampled.csv

用法：

- python -m feature.subsample [每省样本数]

🔹 model/（模型）

rf.py

功能：

- 随机森林训练

- 精度评估（R² / RMSE）

- 特征重要性分析

- SHAP解释（可选）

输入：

- data/feature/

输出：（暂时未定）

- data/result/

要求：

- 必须划分训练集/测试集

- 必须输出评估指标

- 必须支持复现（随机种子固定）

🔹 utils/（通用工具）

功能：

- 日志记录

- 配置读取

- 通用函数（路径、IO等）

要求：

- 所有模块禁止重复实现工具函数

- 所有日志必须统一格式输出

🔹 main.py（主入口）

功能：

- 控制整个流程执行顺序：

要求：

- 各步骤可独立开关

- 支持断点续跑

- 禁止硬编码路径

**3.3 data 目录**

🔹 inventory/（样地蓄积量数据）

根据数据来源的不同，每个来源以单独的文件夹存放

查阅ProjCode\\data\\inventory\\README.md

🔹 Sentinel2/（遥感数据）

查阅ProjCode\\data\\Sentinel2\\README.md

🔹 DEM/（高程地形数据）

查阅ProjCode\\data\\DEM\\README.md

🔹 feature/（特征数据）

查阅ProjCode\\data\\feature\\README.md

🔹 result/（模型输出结果）

查阅ProjCode\\data\\result\\README.md

**3.4 项目结构强制规范**

1.  data 目录只存数据，禁止存代码

2.  所有中间结果必须落盘（保证可复现）

3.  每个模块必须明确输入/输出

4.  文件命名必须规范（禁止临时命名）

5.  所有路径必须通过 config 管理

6.  data子目录README.md严格按照目录结构对数据进行解释

7.  data子目录README.md要包括对所负责目录下数据的详细介绍，数据处理后的命名规则

**4. 开发规范**

1.  所有新增模块必须在本文件登记，同时记录于开发日志

2.  所有结构调整必须记录原因

3.  禁止"只改代码不记录"

4.  每一步开发必须写"下一步计划"

5.  本项目解释器环境为**项目本地 `.conda`**（`E:\GradProj\ProjCode\.conda`，Python 3.13；依赖见 `environment.yaml`），禁止未经允许随意创建新 conda 环境

6.  在处理roi操作时，应当按照类似于\[类别\]\_\[变量名\]\_\[时间/年份\]\_\[ROI编号\].\[格式\]这种方式，标记好roi_id等信息，保证数据处理来源和去向清晰

**5. 项目维护协议（可复用技能）**

本项目定义一组"维护协议（Skill）"，用于在项目结构发生变动时，

快速、标准化地更新文档和说明，避免遗漏和混乱。

当向 AI 或协作者发出简化指令时，可直接调用对应 Skill，

无需重复描述完整需求。

**【Skill 名称】**

**README同步更新**

**【触发条件】**

**当项目结构发生变化：**

**- 新增/删除文件夹**

**- 新增脚本文件**

**- 修改文件或文件夹用途**

**- data/目录结构调整/变化**

**【执行内容】**

**1. 更新根目录 README.md：**

**- 更新项目结构树**

**- 更新文件/文件夹说明**

**2. 更新 data/README.md：**

**- 更新数据目录整体说明**

**3. 更新 data 子目录 README.md（若存在）：**

**- Sentinel2/**

**- DEM/**

**- feature/**

**- inventory/**

**- result/**

**- 以上子目录
README.md对所属目录及其下级子目录负责，应当包含对数据类型、内部/外部格式、输入来源、数据获取方式、输出去向、子数据目录结构，数据时间/空间范围等尽可能的详细介绍**

**4. 更新 config/README.md（若存在）**

**5. 更新各模块目录 README.md（若存在）：**

**- downloader/**

**- preprocess/**

**- feature/**

**- model/**

**- utils/**

**- 以上各模块目录README.md，内含其模块目录下各脚本的功能、输入/输出格式**

**【执行约束】**

**- 仅修改受影响部分**

**- 不重写整个 README**

**- 保持原结构不变**

**【输出要求】**

**所有 data 子目录 README.md
充分服务于对数据的介绍，应当严格按照目录结构进行介绍**

**所有 模块目录 README.md 充分服务于对模块的介绍**

**【调用方式】**

**👉 执行README同步**

**6. 开发日志（强制规范）**

1.  开发日志路径：ProjCode/DEVELOP_LOG.md

2.  所有变动/操作都应记录于开发日志

3.  应严格遵循开发日志模板，

4.  每一个开发步骤，如非具体指明或具体安排，均由上一条开发日志而启，以本条开发日志而终

**7. 补充说明**

1. 项目根目录 **项目开发文档.md** 为项目详细信息与正确引导的唯一维护文件（不再使用 .docx 同步展示）。

**8. 研究过程决策记录**

> 本节记录研究过程中的关键决策、依据与结论，随研究推进持续追加（每条标注日期）。
> ⚠️ 8.1~8.8 为**芬兰阶段（已终止）**历史决策，保留备考；**8.9 起研究区切换为西班牙 IFN4**。

---

**8.1 数据探索：Finland_NFI_2023_GSV 关键事实（2026-08-09）**

已下载数据为芬兰 Luke 的多源国家森林清查（MS-NFI）2023 全国蓄积量栅格，共 4 个主题（总/松/云/桦，m³/ha）：

- 空间：EPSG:3067，16m，42240×73472，覆盖芬兰全国（lon 15.50°E~33.13°E，lat 59.33°N~70.11°N）
- 时间：2023 年度（野外数据更新至 2023-07-31）；发布于 2025-08-05
- 格式：GeoTIFF，单波段 uint16，LZW 压缩，tiled 512×512，内部 overviews，nodata=32767
- 生产方法：VMI13 地面样地（51051 个）+ Sentinel-2 L2A 反射率镶嵌影像，增强 k-NN（5 邻）估计
- 许可：CC BY 4.0（使用需注明 ©Luonnonvarakeskus, 2023）
- 详细登记见 `data/inventory/README.md`

---

**8.2 决策一：蓄积量数据的时间维度——是否可分季节（2026-08-09）**

- 问题：MS-NFI 是持续多年监测吗？蓄积量能否按季节划分？
- 分析：
  - MS-NFI 是多年系列（每 1~2 年一期，2005~2023），但**每期是独立的年度快照**，本数据仅为 2023 年一期
  - 野外数据统一更新到 2023-07-31 单个时点，输出为单一全年蓄积量图
  - 蓄积量是"存量变量"，一年内变化远小于物候波动，不存在有意义的季节区分
- **结论：蓄积量标签为年度单一值，不分季节 → 分季节建模无意义；Sentinel-2 特征也应为匹配时点的单一生长季时相（不需要多时相重复影像）**

---

**8.3 决策二：Sentinel-2 影像时间选择（2026-08-09）**

- 推荐窗口：**2023-07-01 ~ 2023-08-10**，云量 <20%（可放宽至 <30%），L2A，每 tile 取云量最少一期
- 依据：
  - 与标签时点（2023-07-31）对齐
  - 7 月为芬兰树冠完全展开、LAI 峰值、叶绿素最稳定期，光谱-蓄积量关系最稳定
  - 避开 6 月树叶未展开、8 月末~9 月秋季变色的物候拐点
  - 夏季云量全年相对最低；芬兰高纬重访频繁（约 2~3 天/次），7 月内必有候选
- 南北差异：南部可放宽至 7 月上旬~8 月上旬；北部拉普兰更应锁定 7 月中~8 月初

---

**8.4 决策三：研究区与数据源选择（芬兰 vs 秦岭）（2026-08-09）**

- 芬兰森林组成分析（基于 MS-NFI 全图采样）：
  - 全国整体偏针叶纯林：松 ~51%、云杉 ~30%、桦木 ~17%（蓄积量占比）
  - 南北梯度：南部（~60–63°N）云杉占比高、相对最混交；中部松占比上升；北部拉普兰松占绝对主导、林线附近桦木增多
  - 局部有纯松/纯云杉/纯桦林，但大范围人工纯林少，多为"优势树种+少量伴生"
- 对比：
  - 秦岭：真正针阔混交、树种丰富、**实测真值**，但样本少、需同源纯林、遥感已就位（陕西 107°E/34.3°N）
  - 芬兰：混交度低（严格说是偏针叶混交）、标签为遥感估计（同源偏差），但**同源自洽、样本量巨大**
- **方法论铁律：纯林 vs 混交林对比必须在同一数据体系内进行**，否则结论被数据源差异污染
- **结论：以芬兰 MS-NFI 为建模主战场（内部自动划分纯/混），秦岭实测样地作为独立真值验证/补充研究**

---

**8.5 决策四：纯林 vs 混交林实验设计（2026-08-09）**

- 研究问题拆解：
  - A：混交林与纯林哪个更容易预测（光谱-蓄积量关系稳定性）
  - B：分开建模 vs 合并建模哪个更好（是否值得按林型分模型）
  - 建议一次实验同时回答 A 与 B
- 关键方法约束：
  1. **同源划分**：用 MS-NFI 树种栅格自动划分——纯林区（单树种蓄积量占比 >90%）、混交区（松/云/桦占比较均衡）
  2. **空间分块交叉验证**：逐像元采样空间自相关强，随机划分会虚高精度 → 按 tile/子区整块划分训练/测试（或空间缓冲去重）
  3. **同源偏差声明**：MS-NFI 是"参考标签"（由 Sentinel-2 估计），精度会被高估，对比结论聚焦相对差异（RMSE%），必要时用独立地面样地验证绝对精度
  4. **相对指标**：两组蓄积量水平可能不同，必须用 RMSE%（=RMSE/均值）而非绝对 RMSE
- 实验矩阵：
  - 模型① 混交模型：混交样本训练 → 混交测试评估
  - 模型② 纯林模型：纯林样本训练 → 纯林测试评估
  - 模型③ 全局模型：全样本训练 → 分别看混交/纯林测试精度
  - 指标：R²、RMSE、RMSE%、MAE；固定随机种子
- 预期结论（有文献支持）：纯林结构均匀、关系稳定，**纯林精度通常高于混交林**；可为混交林单独建模提供依据

---

**8.6 决策五：是否寻找更合适的数据（2026-08-09）**

- 理想数据标准：真值标签 / 同一体系内纯/混同源 / 样本量充足 / 配套 2023 Sentinel-2 / 树种组成清晰 / 开放可获取
- 现实调研结论：
  - 芬兰 VMI 地面样地（实测真值）：**不公开**，需申请研究许可，周期长、不确定
  - 中国 NFI / 一类清查：**不公开**
  - 瑞典/挪威 NFI 样地：原始样地同样不公开
  - 现有 MS-NFI 栅格：可用但存在"混交度低 + 同源偏差"两个局限
- 决策（三路径）：
  - **路径 A（主线，推荐）**：芬兰 MS-NFI 内部纯/混对比 + 秦岭实测做独立小样本真值验证（零新增获取成本）
  - **路径 B（并行，低优先）**：申请芬兰 VMI 研究许可样地，作为独立真值验证集（不阻塞主线）
  - **路径 C（条件启用）**：以秦岭为混交主战场，前提是确认秦岭 xlsx 样地放回 + 找到同源（中国）实测纯林
- **结论：先不投入大量时间寻找新数据，把现有数据路径走通，并行做低成本补充调研**

---

**8.7 当前决策汇总**

| 决策项 | 结论 |
|--------|------|
| 蓄积量数据时间维度 | 年度单一值，不分季节 |
| Sentinel-2 影像时间 | 2023-07-01 ~ 08-10，云量<20~30%，每 tile 取云量最少一期 |
| 主研究区/数据源 | 芬兰 MS-NFI（同源纯/混划分）+ 秦岭实测验证 |
| 实验设计 | 同源划分 + 空间分块 CV + RMSE% 对比 + 三模型矩阵（混交/纯林/全局） |
| 是否寻找新数据 | 主线用现有数据；VMI 研究许可作为并行加分项 |

---

**8.8 遗留问题：蓄积量标签数据质量（2026-08-09）**

- 遗留事项（**暂缓处理，不阻塞当前工作**）：
  - MS-NFI 蓄积量标签为**遥感估计值**（由 Sentinel-2 + k-NN 产出），与遥感特征**同源** → 用它训练 RF 存在精度高估风险（同源偏差）
  - 芬兰混交区"混交度低"（偏针叶优势 + 桦木伴生），是否足以代表"典型混交林"存在争议
  - 是否申请芬兰 VMI 地面样地（实测真值）作为独立验证集 —— 待定
- 处理策略：
  - 当前**先不解决标签质量问题**，优先推进：确定纯林/混交林典型区域 → 采集对应 Sentinel-2 影像 → 打通"影像 → 特征 → 模型"数据链路
  - 影像采集与标签问题**相对独立**，不互相阻塞
  - 待数据链路跑通、研究区确定后，再回头评估标签质量对结论的影响（必要时申请 VMI 样地 / 引入秦岭实测验证）
- 备注：一旦有新决策或数据进展，在本节追加更新并撤销本条"遗留"状态
我写的：最近进展，数据将采用西班牙的数据，能区分混交和纯林，网站已经收藏

---

**8.9 决策六：研究区切换为西班牙 IFN4（2026-09-18）**

- 切换动因（解决 8.8 的两项遗留）：
  - 芬兰 MS-NFI 蓄积量标签为**遥感估计值**（与特征同源）→ 精度易被高估；
  - 芬兰"混交区"混交度低，纯林/混交对比代表性不足。
- 新数据源：**西班牙 IFN4**（Cuarto Inventario Forestal Nacional，MITECO 公开）：
  - **实测野外样地**（真值标签），且含**逐木树种信息**（`PCMayores.Especie`）→ 可明确区分纯林/混交；
  - 三省：**León（IFN4 2019，优先）/ Burgos（2018）/ Lugo（2009）**；
  - 蓄积量须由树木表（Especie / Dn1 胸径 / Ht 树高）按 IFN4 官方材积公式计算（见 `2.MANUAL_PROCESO_DATOS_IFN4.pdf`）；
  - 坐标：样地在 `PCDatosMap`（CoorX/CoorY/Huso）；**León 的 Huso 字段存在误标，实测均为 EPSG:25830**。
- 遥感数据：`python -m downloader.spain` 采集三省 S2 L2A —— **25 个整 tile / 21.26 GB**，解压 SAFE 已完成：
  - 时相：León 2019 / Burgos 2018 / Lugo 用 2016 替代（2009 无同期 S2）。
- 研究问题调整：
  - 由"纯林 vs 混交林"（芬兰）转为**省份间对比 + 分省/合并建模**；
  - 若 IFN4 树种信息足够，可继续保留**纯林/混交**分组（且标签为实测，说服力更强）。
- 阶段处理：芬兰专用数据与代码已清理；历史开发日志归档至 `DEVELOP_LOG_ARCHIVE.md`，
  `DEVELOP_LOG.md` 归零以便后续记录。

---

**8.10 当前决策汇总（2026-09-18 更新）**

| 决策项 | 结论 |
|--------|------|
| 研究区 / 数据源 | **西班牙 IFN4**（实测样地；León 优先 / Burgos / Lugo） |
| 蓄积量标签 | 由 IFN4 树木表按官方材积公式计算（非遥感估计） |
| Sentinel-2 | 按省采集 L2A（已下载 25 tile / 21.26 GB），时相与调查年匹配 |
| 研究问题 | 省份间对比 + 分省/合并建模（可选保留纯林/混交分组） |
| 建模评估 | 空间分块 CV（KMeans + GroupKFold）+ R²/RMSE/RMSE%/MAE |
| 待办 | 西班牙 DEM 获取；IFN4 样地蓄积量计算；研究区范围确定；特征提取实现 |

---

**8.11 研究方案 v1：特征体系与创新点（2026-09-19）**

> 来源：`docs/研究方案_探讨记录.md`（探讨过程与收敛稿）经技术核验后的**整合版**；
> 本节点为后续特征工程与论文写作的依据。

**A. 方案定位（预期管理）**

| 维度 | 结论 |
|------|------|
| 做什么 | 不是"提出新方法"，而是**标准框架下的有效增强**（Method Enhancement） |
| 创新类型 | 遥感反演中的**特征工程优化型创新** |
| 创新级别 | 特征工程改进（非新模型 / 非新算法） |
| 适配度 | 本科毕设最稳、最合理的创新类型 |

**B. 模型框架（保持不变）**

```text
Sentinel-2 + DEM → 特征工程 → Random Forest
```

**C. 特征体系（三层结构 · 修正后自洽版）**

> ⚠️ 原方案称"总特征数 ≈ 20~30"，但若对所有变量都做多尺度 mean/std 会膨胀到 **55 个**（算术不自洽）；
> 且 **25m 窗口在 10m 影像上仅 2.5 像元**（不足 3×3，统计不稳定）。
> 下表为**修正并定死**的清单：

| 组 | 计算方式 | 特征 | 数量 |
|---|---------|------|------|
| ① 基础光谱 | 30m 窗口（3×3）均值 | B02 B03 B04 B05 B06 B07 B08 B8A **B11 B12** | 10 |
| ② 植被指数 | 同上 | NDVI NDWI NDRE | 3 |
| ③ 多尺度（受控） | 50m 窗口（5×5）**std**，仅关键变量 | NDVI_std50 NDRE_std50 B8_std50 | 3 |
| ④ 结构-纹理 | NDVI → GLCM **7×7**、16 灰度级、4 方向均值 | contrast entropy | 2 |
| ⑤ 结构-邻域 | 100m 窗口（10×10） | NDVI_mean100 NDVI_std100 | 2 |
| ⑥ 地形（DEM 就绪后启用） | 30m 窗口 | elev slope aspect | 3 |
|  |  | **合计** | **23** |

- **尺度依据**：IFN4 样地为同心圆、**最大半径 25m** → 50m 窗口（±25m）与样地边界一致；
  **100m 邻域属"景观背景信息"**，非样地自身属性，论文须论证其引入的合理性。
- **红边说明**：B5/B6/B7 为 **20m** 波段，重采样到 10m 不增加信息（仅为插值），需在论文中说明。
- **SWIR 决策（2026-09-19）**：纳入 **B11/B12**（SWIR，文献中对生物量/蓄积量贡献显著）
  → 基础光谱 8 → 10 个，累计 **23 个特征**；创新点 3"数据约束"依然成立于"S2 + DEM"层面。

**D. 创新点（正式表述）**

> **核心创新（一句话）**：在不引入额外数据源的前提下，通过引入空间结构特征（纹理 + 邻域统计），
> 提升森林蓄积量估测精度。

| # | 创新点 | 说明 |
|---|--------|------|
| 1 | **结构信息引入（主要）** | 传统：仅用光谱（NDVI 等）→ 本文：光谱 + **NDVI 纹理（GLCM）+ NDVI 邻域统计**；本质是从"**光谱信息**"走向"**结构信息**"，表达林分内部异质性与空间连续性 |
| 2 | **多尺度受控设计** | 不盲目做多尺度，**只对关键变量**（NDVI / NDRE / B8）做 50m std → 避免高维冗余、提高模型稳定性 |
| 3 | **数据约束下的特征设计** | 在**仅使用 Sentinel-2 与 DEM** 的条件下构建高效特征体系（答辩时很有用） |

**E. 实验设计**

| 组 | 特征组合 |
|---|---------|
| ① baseline | 光谱 |
| ② + 多尺度 | 光谱 + 50m std |
| ③ + 结构（创新） | + GLCM 纹理 + 100m 邻域 |

> ⚠️ **修正**：原设计为**累积式**，无法区分"纹理"与"邻域"各自的贡献 →
> 须补做**单变量消融**（分别去掉纹理、去掉邻域各跑一次），以量化各自贡献。
> 评估沿用 `model/rf.py` 的 **KMeans + GroupKFold 空间分块 CV**（比原方案描述更规范）。

**F. 论文 / 答辩常见质疑与应对（备查）**

| 质疑 | 回应 |
|------|------|
| "这不就是别人做过的？" | 别人做的是"**类似**"，本文做的是**组合 + 约束优化 + 系统验证** |
| "没有深度学习，是否不够创新？" | 本任务样本规模下，**RF + 特征工程 > 硬上深度学习** |
| "这不是大家都知道的吗？" | 现有研究**要么只做光谱、要么做复杂模型**；本文在**数据受限条件下系统验证了结构特征的有效性，并通过消融实验量化其贡献** |

**G. 落地前置条件（按优先级）**

| # | 事项 | 状态 |
|---|------|------|
| 1 | **计算 IFN4 样地蓄积量 y**（`PCMayores` → 官方材积公式） | ⛔ **唯一真实阻塞**（无 y 则全流程无法运行） |
| 2 | 特征清单与窗口最终确认（见 C 表） | ✅ 已确认（含 SWIR 纳入决策） |
| 3 | CV 与消融策略定稿（空间分块 + 单变量消融） | 待定稿 |
| 4 | 西班牙 DEM 获取（⑥ 地形特征依赖） | ⏳ 待获取 |
| 5 | 说明性事项：红边 20m、León 省内时相跨 2019-07-19 ~ 09-12（物候差异） | 论文需声明 |
