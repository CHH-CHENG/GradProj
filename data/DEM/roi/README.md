# DEM 裁剪数据目录

放置按研究区/样地裁剪后的 DEM 数据，用于与遥感影像对齐后提取地形特征。

## 放置的数据
- 裁剪后的 DEM 高程栅格（⏳ 尚未生成）

## 数据类型
- 地形高程栅格（GeoTIFF）

## 数据来源
- 由 `DEM/raw/` 中的原始 DEM 按研究区/样地范围裁剪得到

## 数据内部格式
（待补充：分辨率、投影、高程单位、范围等，应与原始 DEM 保持一致）
